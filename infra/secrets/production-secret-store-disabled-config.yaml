# Step 53 -- Disabled Production Secret Store Config (Stage 55A).
# Placeholder proving production secret management is fail-closed today. No real
# provider, endpoint, credential, project ID, vault path, or tenant.

version: "1"

productionSecretStore:
  enabled: false
  configured: false
  provider: disabled
  failClosed: true
  readSecretValuesEnabled: false
  writeSecretValuesEnabled: false
  rotationEnabled: false
  productionReady: false
  requiredBefore:
    - production_oidc
    - production_session_key_rotation
    - production_backup
    - production_gitops_repo_credentials
    - production_deployment
  status:
    ready: false
    reason: provider_not_configured
