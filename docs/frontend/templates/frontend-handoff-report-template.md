# Frontend Handoff Report Template

> Copy into `docs/frontend/<stage>/handoff-report.md` and fill in when frontend work is ready for
> Claude Code's integration review. Owner: Codex.

## Stage

`<stage id>`

## What was implemented

Summary of components/routes/API-client changes shipped.

## Deviations from the contract or design brief

List any, with reasoning. If none, state "none".

## Test evidence reference

Link to `docs/frontend/<stage>/test-evidence.md`.

## Known gaps

Anything explicitly deferred.

## Ready for integration review?

`yes` / `no` — if `no`, state what's blocking.

## Statement

Frontend implementation report only. No backend/database/workflow/policy/infra change. No
production action.

---
_Non-production only. No production action. No production data. Do not include internal IP
addresses, SSH aliases, private hostnames, real tokens, credentials, private URLs, or environment
secrets — use neutral labels such as "test host", "internal test runtime", "admin console local
tunnel", "sandbox repo"._
