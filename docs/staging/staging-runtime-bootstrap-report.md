# Staging Runtime Bootstrap Report (Step 64B.2B)

> **Staging only — non-production only. No production action. No production secret. No external write.**
> **Live GitHub / Slack (Discord) / LLM integrations disabled or mocked. Demo workflow NOT executed (that is Step 64D).**

Overall result: **PASS**. The AI Agents Platform **staging** runtime was bootstrapped on the
staging target host `10.0.1.32` (`agentai-swd-stage`) under explicit operator authorization,
using the committed staging compose stack. This is a staging bring-up, **not** a production
deployment.

## Target host / repo
- **Target host:** `10.0.1.32` (`agentai-swd-stage`), Ubuntu 24.04.4 LTS.
- **Access:** key-based SSH (`itadmin`); no password used or exposed; private key never
  printed / committed / stored.
- **Repo path:** `/data/ai-agents-staging/AI-Agents-SWD` (cloned from `origin/main`).
- **Deployed commit:** `f43e163` (branch `main`).
- **Compose file:** `infra/docker-compose/docker-compose.staging.yml`, project
  `aiagents-staging` (host ports `+10000` offset, bound to `127.0.0.1`).
- **Env file:** `infra/runtime/.env.staging.local` — **gitignored, `chmod 600`, never
  committed, contents never printed.** Generated by `scripts/generate_staging_env.sh` (random
  `POSTGRES_PASSWORD`; token fields keep sandbox placeholders). `SECRET_PROVIDER=mock-vault`;
  `ALLOW_VAULT_DEV_MODE_FOR_STAGING=true` (documented staging escape hatch — not production
  secret storage).

## Bootstrap sequence (via `scripts/start_staging_runtime.sh`)
1. Generated the gitignored staging env + mock-vault fixture.
2. `validate_runtime_config.sh --mode staging` → `RUNTIME_CONFIG_VALIDATION: PASS`.
3. `docker compose -p aiagents-staging -f docker-compose.staging.yml --env-file … config` →
   valid (28 top-level keys). Base images pre-pulled; 15 app images built from source.
4. `docker compose … up -d` → 22 containers started.
5. Waited for Postgres (`pg_isready`) + Redis (`PONG`); applied all `migrations/*.sql`
   (idempotent); initialised Redis Streams consumer groups; restarted consumer services.
6. `START_STAGING_RUNTIME: PASS`.

## Services started
- **22 containers running** (21 report `healthy`; `vault` runs in dev mode with no
  healthcheck). Full table: [staging-runtime-service-status.md](staging-runtime-service-status.md).

## Ports
- All host bindings on **loopback** (`127.0.0.1`), `+10000` offset. Orchestrator (Admin
  Console) `127.0.0.1:18000 → 8000`; postgres `15432`; redis `16379`; policy `18001`;
  approval `18002`; audit `18003`; communication-gateway `18004`. **Not exposed on the LAN.**

## Health check results
| Check | Result |
|---|---|
| orchestrator `GET /health` | **200** |
| orchestrator `GET /admin` → `/admin/` | 307 → **200** ("Admin Console v0 — read-only") |
| policy-engine `:18001/health` | **200** |
| approval-engine `:18002/health` | **200** |
| audit-service `:18003/health` | **200** |
| communication-gateway `:18004/health` | **200** |
| postgres `pg_isready` | accepting connections |
| redis `PING` | `PONG` |
| `GET /operations/safety` | **200** |

## Admin Console access
- Host-local: `http://127.0.0.1:18000/admin` (renders the read-only Admin Console v0).
- Operator: SSH local port-forward — see
  [staging-admin-console-access-evidence.md](staging-admin-console-access-evidence.md).

## Safety endpoint result summary (`/operations/safety`)
- `production_executed_true_count = 0`
- `deployment_environment_production_count = 0`
- `workflow_production_executed_true_count = 0`
- `github_external_write_enabled = false`, `real_github_test_enabled = false`
- `discord_external_send_enabled = false`, `discord_real_test_enabled = false`,
  `discord_has_token = false`
- `github_has_token = true` — a **sandbox/mock** token from the mock-vault fixture; live write
  and real-test gates are **off**, so no live GitHub write occurs. Noted as a limitation, not a
  live integration. See [staging-runtime-known-limitations.md](staging-runtime-known-limitations.md).

## Known gaps / limitations
See [staging-runtime-known-limitations.md](staging-runtime-known-limitations.md) (Vault dev
mode, mock-vault provider, sandbox GitHub token, no swap, HTTP-only demo, demo workflow not
run).

## Stop / restart
See [staging-runtime-stop-and-restart-notes.md](staging-runtime-stop-and-restart-notes.md).

## Explicit safety statements
- **No production action** — no production deploy, no production sync, no production secret, no
  external write, no GitHub merge, no image push, no registry login. `production_executed_true_count=0`.
- **Live integrations disabled / mocked** — GitHub live write off, Discord live send off, LLM
  live call disabled/mocked, secrets via mock-vault.
- **Demo workflow NOT executed** in this stage (Step 64D).

---
_Staging only — non-production only. No production action. No production secret. No external write._

<!-- staging-safety: staging-only=true non-production=true production-action=false production-deploy=false production-sync=false production-secret=false external-write=false github-merge=false image-push=false production-ready=false credential-storage=false runtime-deployment=staging-only live-integrations=disabled demo-workflow-executed=false -->
