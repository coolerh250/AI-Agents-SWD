# ADR-NNNN: `<short title>`

> Copy into `docs/decisions/NNNN-<slug>.md`, replacing `NNNN` with the next sequential number.

## Status

`proposed` / `accepted` / `superseded by ADR-NNNN`

## Context

What situation or constraint prompted this decision? Which stage(s) does it affect?

## Decision

State the decision plainly, one or two sentences.

## Consequences

- What this makes easier.
- What this makes harder or defers.
- Any gap this creates, and which future stage (if any) is expected to close it.

## Alternatives considered

Briefly, and why they weren't chosen.

## Statement

Decision record only. No production action implied unless explicitly stated otherwise in this
document.

---
_Non-production only. No production action. No production data. Do not include internal IP
addresses, SSH aliases, private hostnames, real tokens, credentials, private URLs, or environment
secrets — use neutral labels such as "test host", "internal test runtime", "admin console local
tunnel", "sandbox repo"._
