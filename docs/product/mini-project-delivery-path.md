# Mini Project Delivery Path — Roadmap

This is the roadmap from the project-planning foundation to an end-to-end mini
project delivery pilot. Each step builds on the previous one.

| Step | Title | Status | What it adds |
|---|---|---|---|
| 43 | Project Planner & Task Graph | **closed** | brief, user stories, acceptance criteria, milestones, work-item graph, dependency validation, planning-only orchestration + operations visibility |
| 44 | Agent Discussion & Design Review Protocol | **closed** | structured multi-role discussion + design review + gates + go/no-go on a project graph, review-only, before any code |
| 45 | Real Repo Workspace Operator v1 | **closed** | a controlled, allowlisted workspace operator that generates a FastAPI Todo project, runs pytest + static checks, and produces diff / artifacts / work-item execution links — no repo write, no PR, no deploy, no real LLM |
| 46 | Mini Project Delivery Pilot | **closed** | chains plan → design review → controlled workspace → test/static evidence → acceptance evaluation → QA summary → safety summary → mini delivery pilot report, controlled-only |
| 47 | Delivery Package & Acceptance Gate | **closed** | assemble a formal delivery package (14 sections + artifacts), gate it (18 acceptance checks), produce handoff summaries + readiness snapshot + operator-review placeholder; `ready_for_operator_review`, human acceptance pending, controlled-only |
| 48 | Admin Console v0 (read-only) | **closed** | read-only browser UI (`/admin`) + 6 aggregate `/operations/admin-console/*` endpoints over projects / reviews / workspaces / pilots / delivery packages / safety / regression / incidents / LLM; no write API, no operator actions, secret/CoT redaction |
| 49 | Backup / DR Gap Closure | **done (this stage)** | close the four backup/DR gaps (encryption_no_key, storage_not_off_host, schedule_dry_run_only, migration_down_gaps) at a controlled test baseline → readiness `passed_with_non_production_limitations`; no production backup/restore, no real cloud, no real schedule |
| 50 | Admin Console v1 (operator actions) | **done (this stage)** | controlled operator actions (accept / reject / request-changes / note / allowlisted verification rerun) gated by test-local auth + RBAC + CSRF + policy + confirmation + idempotency + audit; high-risk actions disabled; acceptance is human-review only (no deploy/PR/prod) |
| 51 | Kubernetes / Helm / ArgoCD Runtime Baseline | **closed — static baseline validated, not deployed** (51.1–51.4) | container runtime + GitOps deployment baseline; validated, NOT deployed (no cluster, no Helm install, no ArgoCD sync, no production readiness) |
| 51.1 | Runtime Inventory & Helm Foundation | **done (this stage)** | evidence-backed runtime inventory + dependency matrix + component catalog; values-driven Helm foundation chart (generic Deployment/Service/ConfigMap/ServiceAccount) across dev/test/staging-placeholder/prod-placeholder; fail-closed non-deployable production placeholder; lint + render verified; no cluster, no deploy |
| 51.2A | Workload Security & RBAC Safety Baseline | **done (this stage)** | restricted SecurityContext baseline (runAsNonRoot, RuntimeDefault seccomp, no privesc, drop ALL, read-only root, size-limited emptyDir writable paths), ServiceAccount token automount off, RBAC safety (no Role/ClusterRole, no Kubernetes API access); static manifest baseline, no cluster |
| 51.2B | NetworkPolicy & Service Connectivity Baseline | **done (this stage)** | revalidated dependency matrix (75 edges; 49 internal + 26 observability-deferred), connectivity catalog, default-deny ingress/egress, scoped DNS, per-target ingress / per-source egress for all 49 internal edges, Postgres/Redis isolation, external egress disabled, fail-closed; no cluster |
| 51.2C1 | Storage Ownership & Data Lifecycle Baseline | **done (this stage)** | evidence-backed storage consumer inventory + data lifecycle; typed store ownership; generated RWO PVCs for in-cluster Postgres/Redis in dev/test only (Deployment volume integration); workspace ephemeral per-pod (no RWX); reports/audit exports unresolved; backup separate + deferred; fail-closed (no StorageClass/PV, no hostPath/NFS, no real storage class/claim); no cluster |
| 51.2C2 | Migration, Backup & Restore Job Baseline | **done (this stage)** | evidence-backed batch operation inventory + risk; fixed shell-free command catalog; controlled migration Job (advisory lock, forward-only), disabled+suspended backup CronJob (Forbid, secretKeyRef-only, disabled target), disabled restore Job scaffold (isolated `aiagents_restore_drill_` target, source≠target); token-off batch ServiceAccounts (no RBAC); minimal DB-only NetworkPolicy; fail-closed staging/production; templates validated, NOT executed; no cluster |
| 51.3 | ArgoCD & Environment GitOps Baseline | **done (this stage)** | ArgoCD AppProject (restricted source/destinations, no cluster-scoped/Secret) + dev/test/staging/production Application manifests (values-pinned) + non-production app-of-apps (dev+test only) + environment catalog + policy summaries; auto-sync/prune/selfHeal/hooks/finalizers/credentials all absent; production disabled placeholder excluded from app-of-apps; validated, NOT applied (no ArgoCD, no sync, no cluster) |
| 51.4 | Runtime Visibility & Integrated Verification | **done (this stage)** | read-only runtime baseline SDK + 12 GET `/operations/runtime/*` endpoints + `/operations/safety` Kubernetes/Helm/GitOps fields + Admin Console read-only Runtime Baseline view (no deploy/sync/apply) + combined Step 51 verification (`KUBERNETES_HELM_ARGOCD_BASELINE_VERIFY`); `runtime_production_ready=false`, `runtime_validated_not_deployed=true`, `production_executed_true_count=0`; no cluster, no sync |
| 52 | Production Identity & OIDC Foundation | **closed — modeled, fail-closed, not enabled** (52.1–52.4) | identity boundary model → OIDC abstraction → session hardening / role mapping → identity visibility + integrated verification; production identity NOT enabled (no real IdP, no production auth, no production login) |
| 52.1 | Identity Inventory & Auth Boundary Model | **done (this stage)** | evidence-backed authentication/session/CSRF/RBAC/operator-action inventories + identity trust boundary model + test-vs-production auth boundary + identity-to-audit mapping + human-acceptance & verification-rerun boundaries + production OIDC prerequisites (unconfigured) + risk register + policy catalog; test-local auth non-production, production auth disabled/fail-closed, platform_admin ≠ infrastructure admin, human acceptance ≠ deployment; no real OIDC, no production auth, no secret committed |
| 52.2 | OIDC Provider Abstraction & Disabled Production Config | **done (this stage)** | model-only OIDC abstraction (`shared/sdk/identity`: provider/config/policy/redaction) + disabled production config + 10 contracts (provider catalog, discovery, JWKS, claim, role-mapping, callback, state/nonce/PKCE, token-validation, safety-policy) + fail-closed loader/validator + 3 verifiers + combined baseline + 15 tests; production OIDC `disabled_unconfigured`, no discovery/JWKS fetch, no callback, no token validation, no real issuer/client ID/secret, unknown user denied, `platform_admin` never auto-granted, token role claim not authoritative; no IdP connection, no production auth, no secret committed |
| 52.3 | Session Hardening & Role Mapping | **done (this stage)** | session hardening catalog + non-destructive cleanup utility (dry-run, never deletes, no raw token; existing schema, no migration) + concurrency/forced-logout/key-rotation models + safe local role mapping engine (`shared/sdk/identity`: role_mapping, session_cleanup, identity_runtime_config) + role-mapping/unknown-user/break-glass/authorization-decision models + audit enrichment plan + 6 verifiers + cleanup verifier + combined baseline + 15 tests (82 cases); unknown user deny, default role none, `platform_admin` explicit-mapping-only, no wildcard groups, no real group IDs, token role claim not authoritative, break-glass disabled, key rotation model-only (production secret store deferred to 53); no real IdP, no production auth, no secret committed, `production_executed=false` |
| 53 | Production Secret Management Foundation | **closed — modeled, fail-closed, not configured** | secret inventory/classification/ownership/usage + reference-only `SecretRef` (no value) + disabled secret-store abstraction (`read_secret_value` raises) + disabled production store config + lifecycle/rotation/access-boundary/audit/redaction models + 4 reference catalogs (all `store=disabled`) + read-only `/operations/secrets/*` (13 GET) + 21 `/operations/safety` secret fields + Admin Console read-only Secret Posture view (no reveal/copy/upload/rotate/configure) + 9 verifiers + combined `SECRET_MANAGEMENT_FOUNDATION_BASELINE_VERIFY` + 23 tests; no real secret store, no secret value, no committed secret, `secrets_production_ready=false`, `production_executed_true_count=0`. Next: Step 54 (Application Security & Supply Chain Baseline), Step 55 (Non-production Kubernetes Runtime Smoke), Step 56 (Real ArgoCD Non-production Manual Sync) — all pending. |
| 52.4 | Identity Visibility & Integrated Verification | **done (this stage — closes Step 52)** | read-only identity posture SDK (`shared/sdk/identity_posture`) + committed anti-drift summary + 13 GET `/operations/identity/*` endpoints + 35 `/operations/safety` identity fields + Admin Console read-only Identity Posture view (no login/connect/configure/toggle/editor/break-glass button) + combined Step 52 verifier (`IDENTITY_FOUNDATION_BASELINE_VERIFY`) + `IDENTITY_OPERATIONS_VISIBILITY_VERIFY`/`ADMIN_CONSOLE_IDENTITY_POSTURE_VERIFY`/`IDENTITY_SAFETY_FIELDS_VERIFY` + 11 tests + full regression; `identity_posture_status=modeled_fail_closed_not_enabled`, `identity_production_ready=false`, OIDC + production auth disabled, `production_executed_true_count=0`; no real IdP, no mutation endpoint/button, no secret committed |
| 54 | Application Security & Supply Chain Baseline | **closed — application security and supply chain baseline modeled, locally verifiable, not production-enforced** | security & supply-chain inventory → scan toolchain → SBOM/image/container → threat model/release risk/integrated verification; NOT a production security gate / release approval / deployment ready / all-risks-remediated. Step 55 (cluster smoke), Step 56 (ArgoCD sync) pending |
| 54.1 | Security & Supply Chain Inventory / Policy Baseline | **done** | application security asset + supply chain + dependency surface inventories + scan policy catalog + SAST/dependency/secret/SBOM/container-image policy models + threat-model & release-risk input catalogs + evidence model + finding taxonomy + fail-closed gate policy (14 catalogs + anti-drift summary) + read-only `security_foundation` SDK + 17 GET `/operations/security/*` + 20 `/operations/safety` security/supply-chain fields + Admin Console read-only Security / Supply Chain view (no run-scan/upload/connect/configure/create-PR/push-image/gate control) + 8 verifiers + combined `SECURITY_SUPPLY_CHAIN_POLICY_BASELINE_VERIFY` + 20 tests; `security_foundation_status=modeled_not_enforced`, `security_production_ready=false`, no scanner run, no SBOM, no image push, no GitHub write, no production gate, `production_executed_true_count=0`. |
| 54.2 | Secret Scan / SAST / Dependency Scan Toolchain Baseline | **done (this stage)** | local scanner capability inventory + scanner execution boundary + scan target catalog + scan exclusion policy + scan result artifact schema + scan status summary model (6 catalogs) + `security_findings` SDK (normalized `SecurityFinding`/`ScanResult`, normalizer, redaction, scan posture) + local runners (secret/SAST/dependency + normalizer; reports to gitignored `.runtime/security/`, never committed) + 9 GET `/operations/security/scans/*` + 16 `/operations/safety` scan fields + Admin Console scan posture section (no run-scan/upload/connect/configure control) + 10 verifiers + combined `SECURITY_SCAN_TOOLCHAIN_BASELINE_VERIFY` (chains Step 51/52/53/54.1) + 19 tests (63 cases); local-only, no external upload/network/token, no CVE lookup, `security_scan_production_ready=false`, `production_executed_true_count=0`. Next: Step 54.3 (SBOM/image/container), 54.4 (threat model/release risk/integrated). |
| 54.3 | SBOM / Image Digest / Container Security Baseline | **done (this stage)** | SBOM capability/boundary/artifact-schema + container image inventory (27 images, no digest pinned, no latest) + image digest/tag policies + Dockerfile security inventory (20 root) + runtime security alignment (Step 51 securityContext vs root-image reality) + image vuln scan capability/result-schema (policy-only, no CVE) + signing/attestation model (disabled) + registry credential boundary (Step 53) + container security evidence model (13 catalogs) + `container_security` SDK + local SBOM + image policy runners (reports to gitignored `.runtime/security/`) + 13 GET `/operations/security/{sbom,images}/*` + 21 `/operations/safety` fields + Admin Console SBOM/container section (no generate-SBOM/pull/scan/login/push/sign/attest) + 12 verifiers + combined `SBOM_CONTAINER_SECURITY_BASELINE_VERIFY` (chains Step 51/52/53/54.1/54.2) + 21 tests (64 cases); local-only, no registry login/image push/signing/attestation, `security_container_production_ready=false`, `production_executed_true_count=0`. Next: Step 54.4 (threat model/release risk/integrated), Step 55 (cluster smoke). |
| 54.4 | Threat Model / Release Risk Summary / Integrated Verification | **done — closes Step 54** | threat model baseline + threat category taxonomy + agent / supply-chain / runtime-gitops threat models + release risk summary model + scoring policy + security evidence package schema + committed integrated summary (9 catalogs) + `security_integrated` SDK + evidence-package / release-risk-summary / readiness-report generators (reports to gitignored `.runtime/security/`) + 9 GET `/operations/security/{threat-model,release-risk,evidence,readiness,step54}/*` + 14 `/operations/safety` integrated fields + Admin Console Threat Model / Release Risk / Evidence section (no generate-evidence/approve-release/enable-gate/deploy/create-PR/sync-ArgoCD) + 11 verifiers + combined `APPLICATION_SECURITY_SUPPLY_CHAIN_BASELINE_VERIFY` (chains Step 51/52/53/54.1/54.2/54.3, deduped) + 17 tests (58 cases) + full regression; release risk summary is NOT an approval, `security_step54_production_ready=false`, no release gate, `production_executed_true_count=0`. Next: Step 55 (cluster smoke), Step 56 (ArgoCD sync). |
| 55 | Non-production Kubernetes Runtime Smoke | **real PASS (scoped) via Step 55.1** | non-production cluster smoke plan + namespace plan + report schema + `values-nonprod-smoke.yaml` + Helm smoke runner (dry-run + guardrails) + `runtime_smoke` SDK + 14 verifiers + combined `NONPRODUCTION_KUBERNETES_RUNTIME_SMOKE_VERIFY` (chains Step 51/52/53/54, deduped) + 12 GET `/operations/runtime/nonprod-smoke/*` + 17 `/operations/safety` smoke fields + Admin Console Non-production Runtime Smoke section (no deploy/helm-install/cleanup/exec/sync) + 10 tests (27 cases); cluster-dependent verifiers were BLOCKED until Step 55.1 stood up a real cluster, now PASS against a live redacted report (never faked), `nonprod_runtime_smoke_production_ready=false`, no production deploy/ArgoCD sync, `production_executed_true_count=0`. |
| 55.1 | Safe Non-production Kubernetes Cluster Bootstrap | **done — real PASS (scoped)** | bootstrap plan + tooling inventory (kubectl v1.36.2 / helm v3.16.4 / kind v0.25.0, official sources, no registry login/push) + kind cluster def (`kind-aiagents-smoke`, local-only, no ingress/LB) + `bootstrap_nonproduction_kind_cluster.sh` (create cluster, `kind load` local images, namespace + non-secret in-cluster secret never committed) + `values-nonprod-smoke-local.yaml` (scoped control-plane subset; postgres PGDATA sub-dir) + real install (6/6 Ready, migration Complete) + `run_nonproduction_runtime_smoke.py` real `kubectl`/connectivity checks → redacted gitignored report consumed by 8 verifiers (strictness raise) + 4 new verifiers + combined `NONPROD_CLUSTER_READY_FOR_RUNTIME_SMOKE_VERIFY` + 5 tests + 5 docs; host RAM upgraded to 15Gi after a first kind-create livelocked the 4Gi/no-swap host (power-cycled, recovered); gaps: scoped subset, kindnet does not enforce NetworkPolicy, migration fail-closed; no production cluster/namespace/registry/ingress/LB/ArgoCD, `production_executed_true_count=0`. Next: Step 56 (Real ArgoCD Non-production Manual Sync) — out of scope, requires explicit operator decision. |
| 56 | Real ArgoCD Non-production Manual Sync | **closed — real ArgoCD non-production manual sync passed, auto-sync disabled** | non-production ArgoCD v2.13.3 in `argocd-nonprod` (ClusterIP only, no ingress/LB/SSO; CRB subject patched; dex/appset/notifications scaled to 0) + restricted AppProject (single dest aiagents-smoke-dev, single source repo, `clusterResourceWhitelist: []`, namespaced kinds only) + Application (manual sync — no `automated` block; public repo chart + values-nonprod-smoke-local.yaml, read-only, no credential) + real manual sync via `kubectl patch` → Synced/Healthy (8 kinds, 6/6 Ready) + manual-sync runner (refuses prod context/ns/auto-sync, no ingress/LB, no token/password output, report never committed) + redacted `.runtime/gitops/` report consumed by verifier + committed summary driving 15 `/operations/safety` fields + 8 GET `/operations/gitops/nonprod-argocd/*` (no sync/install/delete/rollback/promote) + Admin Console read-only ArgoCD section (no mutation button) + 9 verifiers + combined `NONPRODUCTION_ARGOCD_MANUAL_SYNC_BASELINE_VERIFY` + 11 tests (28 cases) + 8 docs; NOT production GitOps/ArgoCD/auto-sync ready; no production cluster/namespace/sync, no GitHub write/image push/registry login, no public ingress/LB, `argocd_production_sync_performed=false`, `production_executed_true_count=0`. Next: Step 57 (Multi-project Delivery Capability & Work-item Dispatch). |
| 57 | Multi-project Delivery Capability & Work-item Dispatch | **closed — multi-project delivery and work-item dispatch baseline completed** | migration 024 extends projects/project_work_items (registry semantics + delivery lifecycle) + new dispatch/events/delivery-state/members/linkage tables + `shared/sdk/projects` + `shared/sdk/work_items` (lifecycle state machine, deterministic dispatch resolver, redacted audit/event builder, safety fields) + 8 `infra/delivery/` policy YAMLs + `multi_project_api.py` (7 GET reads + 3 audited writes under `/operations/delivery/*`, reuse operator test-local auth/CSRF/audit, reason required, production_effect→waiting_approval never dispatched, forbidden targets refused) + comm-gateway mock intake + 11 `/operations/safety` fields + Admin Console Multi-project Delivery page (read + audited create/dispatch, no production/GitHub/ArgoCD/external/promote button) + 10 verifiers + combined `MULTI_PROJECT_DELIVERY_DISPATCH_BASELINE_VERIFY` + 16 tests (46 cases) + 11 docs; deterministic dispatch only, no LLM, no GitHub write/PR, no Jira/Slack/email send, no production deploy/ArgoCD sync, delivery-package-ready≠production approval, work-item completed≠human acceptance, `multi_project_production_ready=false`, `production_executed_true_count=0`. Next: Step 58 (Admin Console v2 Operational Metrics), Step 59 (Sandbox GitHub Draft PR Flow). |
| 58 | Admin Console v2 Operational Metrics | **closed — operational metrics baseline completed** | `infra/operations/` metrics model (11 domains, visibility-only rules, productionReady=false) + source inventory (read-only allowlisted sources; runtime reports never committed; missing-as-stale) + `shared/sdk/operations_metrics` (read-only collectors: DB counts + runtime/committed report reads + config safety; aggregator→redacted snapshot; redaction; freshness; safety fields) + `generate_operational_metrics_snapshot.py`→`.runtime/operations/...json` (gitignored, never committed) + 14 GET `/operations/metrics/*` (GET-only, no generate/refresh/sync/deploy/PR/external-send; runtime/gitops domains degrade to unavailable in-container, host snapshot generator reads `.runtime`) + 10 `/operations/safety` fields + Admin Console v2 dashboard (route `/metrics`, read-only, stale/unavailable shown, no deploy/sync/PR/external-send/production-approve/production-ready/connector control) + 6 verifiers + combined `ADMIN_CONSOLE_V2_OPERATIONAL_METRICS_BASELINE_VERIFY` + 10 tests (22 cases) + 7 docs; visibility only — NOT production readiness/SLA-SLO/multi-tenant, delivery completed≠production approved, security baseline PASS≠all-risks-remediated, no Kubernetes mutation/ArgoCD sync/GitHub write/external send, `operational_metrics_production_ready=false`, `production_executed_true_count=0`. Next: Step 59 (Sandbox GitHub Draft PR Flow). |
| 59 | Sandbox GitHub Draft PR Flow | **closed — sandbox GitHub draft PR flow baseline completed** | 6 committed `infra/github/` policies (draft-PR policy: dry_run default, live_sandbox gated on `SANDBOX_GITHUB_LIVE`+credential, all merge/review/non-sandbox/production-branch/workflow/issue/release/deployment toggles false; repository allowlist: key-only, sandbox-only, no wildcard, draft-PR only; credential boundary: token from env/secret-ref only, never committed/logged/returned, blocked when missing, min scope contents+pull_requests, no admin/workflow/deployment; branch policy: sanitized `sandbox/ai-agents/{project}/{item}/{cid}`, no traversal/space/metachar, never protected branch; PR metadata: `[Sandbox][Draft]` title + required caveats + forbidden secret/token/prompt/CoT; audit mapping: 6 events, required linkage + production_executed=false) + `shared/sdk/sandbox_github` (models/policy/allowlist/branch/pr_metadata/dry_run/client/audit/redaction/safety/store; dry_run plan = no side effect, live_sandbox draft-only via GitHub API, NO merge/ready-for-review/workflow/issue/release/deployment method, token read from env at call time only) + migration 025 (`sandbox_github_draft_prs` linkage; row ≠ merge/review/production approval) + `sandbox_github_api.py` (6 read-only GET + 1 controlled `POST /operations/github/sandbox-draft-pr` reusing operator auth/CSRF/reason/audit, repository_key only, production_effect→blocked never a PR) + 16 `/operations/safety` fields + Admin Console Sandbox GitHub Draft PR section (route `/sandbox-github`, read-only; no create/merge/ready-for-review/workflow-dispatch/production-deploy control, no arbitrary-repo input, no token input) + 9 verifiers + combined `SANDBOX_GITHUB_DRAFT_PR_BASELINE_VERIFY` + 12 tests + 9 docs; sandbox-only — NOT production GitHub automation/merge automation/customer-repo/production release ready, live not configured in test (dry_run only, created_count=0), no PR merge/ready-for-review/workflow dispatch/non-sandbox write/production branch/ArgoCD sync/Kubernetes mutation/external send, token never committed/logged/exposed, `sandbox_github_production_ready=false`, `production_executed_true_count=0`. Next: Step 60 (Release & Deployment Governance). |
| 60 | Release & Deployment Governance | **closed — release & deployment governance baseline completed** | 8 committed `infra/release/` models (governance policy: production forbidden, all production-deploy/auto-promotion/github-merge/tag/release/image-push/registry-login/argocd-production-sync toggles false, productionReady false, allowed dev/test/nonprod; release candidate: nonprod default, production_ready false, accepted_nonproduction ≠ prod approval; deployment intent: validate_only/prepare_nonproduction/request_operator_review allowed, deploy/sync/merge/push/release/tag forbidden, never executes; promotion boundary: dev→test→nonprod→operator_review, production allowed:false future-only, no auto-promotion; evidence package: missing blocks readiness, no secret/token/CoT, never production approved; readiness decision: governance not approval, production_ready always false; rollback requirement: plan+evidence required, defining≠triggering; audit mapping: 7 events + production_executed=false) + `shared/sdk/release_governance` (models/policy/candidates/deployment_intent/evidence/readiness/rollback/audit/redaction/safety/store; validates env never-production, builds candidates/intents, redacted evidence, readiness eval, rollback validate; NO deploy/sync/merge/push/external send) + migration 026 (release_candidates/deployment_intents/release_evidence_packages/release_readiness_decisions/release_audit_events; target_environment CHECK never production, production_ready/production_executed default false) + `release_governance_api.py` (11 read-only GET + 2 controlled POST under `/operations/release` reusing operator auth/CSRF/reason/audit, production rejected, intent never deploys) + 13 `/operations/safety` fields + Admin Console Release Governance section (route `/release-governance`, read-only; no production-deploy/argocd-sync/merge/github-release/image-push/production-approve control, no production-ready toggle) + 11 verifiers + combined `RELEASE_DEPLOYMENT_GOVERNANCE_BASELINE_VERIFY` + 12 tests + 11 docs; non-production governance — NOT production deployment/release approval/auto-promotion/production GitOps, no production deploy/argocd production sync/github merge/image push/registry login/release-tag/workflow dispatch, deployment intent never executes, accepted_nonproduction≠production approval, `release_governance_production_ready=false`, `production_executed_true_count=0`. Next: Step 61 (Production Backup/Restore/DR Operations or controlled cleanup review — operator decision). |
| 61 | Production Backup / Restore / DR Operations | **closed — backup / restore / DR operations baseline completed** | 9 committed `infra/dr/` models (operations policy: production restore/failover/backup-mutation/external-upload/cloud-write/argocd-prod-sync/k8s-prod-mutation/cleanup-execution/restore-execution/kind-teardown/argocd-teardown all false, productionReady false, allowed local/dev/test/nonprod; target inventory: 15 targets, restore_allowed_production false for all, no secret/customer-data backed up; artifact classification: 12 classes, database_dump/redis_snapshot commit_allowed false, temporary_trace/build_cache cleanup-allowed, cluster_runtime_state/scheduled_dr_report never auto-cleaned; cleanup review: allowlisted roots only, arbitrary path rejected, dumps/audit/cluster blocked, kind/argocd/active-db/redis scope blocked, never executes; restore plan: validate/dry-run/schema/integrity only, production+overwrite+failover+customer-data forbidden, never executes; restore validation: metadata/schema/redaction/freshness/dry-run, no active overwrite/sync/mutation; DR operation: 6 governance types, production_failover/restore/cross-region/overwrite forbidden, readiness ≠ production DR ready; recovery evidence: redacted, no secret/raw-dump/kubeconfig/token/CoT, production_ready/production_restore_ready false; audit mapping: 9 events + production_restore/failover/executed false) + `shared/sdk/backup_restore_dr` (models/policy/inventory/classification/cleanup_review/restore_plan/restore_validation/dr_operation/evidence/audit/redaction/safety/store; NO restore/failover/teardown/cleanup execution/external upload) + migration 027 (backup_targets/backup_artifacts/cleanup_reviews/restore_plans/restore_validations/dr_operations/recovery_evidence_packages; target_environment CHECK never production, operation_type CHECK never production failover, production_restore/failover/executed default false) + 3 host generators (runtime inventory/cleanup review/restore validation → gitignored `.runtime/backup-dr/`) + `backup_restore_dr_api.py` (12 read-only GET + 2 controlled POST under `/operations/dr` reusing operator auth/CSRF/reason/audit, production rejected, arbitrary path rejected, never executes) + 21 `/operations/safety` fields + Admin Console Backup/Restore/DR section (route `/backup-dr`, read-only; no execute-cleanup/execute-restore/failover/teardown-kind/argocd-sync/cloud-upload control, no production-ready toggle) + 12 verifiers + combined `BACKUP_RESTORE_DR_OPERATIONS_BASELINE_VERIFY` + 13 tests + 12 docs; non-production governance — NOT production DR/restore/failover/data-mutation, cleanup review-only (never deletes), restore plan+validate only (never overwrites active Postgres/Redis), no kind/argocd teardown/argocd sync/external/cloud upload, DR readiness ≠ production DR ready, `backup_restore_dr_production_ready=false`, `production_executed_true_count=0`. Next: Step 62 (Production Deployment Readiness Gate). |

## Current foundation (Steps 43–44)

* Projects, briefs, user stories, acceptance criteria, milestones, work items,
  dependencies, risks, artifacts, graph snapshots (Step 43).
* Deterministic FastAPI Todo template + dependency graph validation (Step 43).
* Planning-only orchestration: project-scale requests route to the
  project-planner-agent; the workflow stops at `project_planned` (Step 43).
* Multi-role design review: discussion sessions, deterministic role
  contributions (no chain-of-thought), six reviewers, acceptance coverage,
  seven review gates, and a go/no-go decision; review-only — the workflow stops
  at `design_reviewed` / `design_reviewed_with_findings` / `design_review_blocked`
  (Step 44).
* Operations API for project / work item / dependency / graph / acceptance /
  delivery-readiness + discussion / design-review / findings / gates /
  go-no-go / acceptance-coverage visibility.
* Controlled workspace operator: after a non-blocked design review, the
  orchestrator requests a controlled workspace execution that generates a
  deterministic FastAPI Todo project under an allowlisted root, runs pytest +
  static checks, and records diff / artifacts / work-item execution links;
  controlled-only — the workflow stops at `workspace_tests_passed` /
  `workspace_tests_failed` / `workspace_execution_failed` and never deploys or
  opens a PR (Step 45).
* Mini project delivery pilot: one controlled run chains plan → design review →
  workspace execution → test/static evidence → evidence-based acceptance
  evaluation → QA summary → safety summary → a mini delivery pilot report, with
  pilot-level steps, evidence, and artifact links; controlled-only — no PR, no
  deploy, no real LLM, `production_executed=false` (Step 46).
* Delivery package & acceptance gate: a completed mini pilot is assembled into a
  formal delivery package (14 sections, linked artifacts, acceptance checklist),
  evaluated by an 18-check acceptance gate, and accompanied by business /
  technical / operator handoff summaries plus a delivery readiness snapshot and a
  pending operator-review placeholder. The gate resolves to
  `ready_for_operator_review`; human acceptance stays `pending` and operator
  action endpoints are disabled by default; controlled-only — no PR, no deploy,
  no real LLM, no external delivery, `production_executed=false` (Step 47).

* Admin Console v0: a read-only browser UI served at `/admin` (zero-build static
  fallback + optional React/Vite bundle) backed by six read-only aggregate
  `/operations/admin-console/*` endpoints, surfacing platform safety, projects,
  task graph, design review, workspace execution, mini delivery pilot, delivery
  package + acceptance gate, human acceptance (pending), regression, backup gaps,
  incidents, and LLM/cost. No write API, no operator actions, secret /
  chain-of-thought redaction; `production_executed=false` (Step 48).

* Backup / DR Gap Closure: closes the four long-standing backup/DR gaps at a
  controlled test baseline — test-only encrypted backup + manifest, mock
  off-host transfer with readback verification, dry-run-validated schedule
  specs, dry-run retention, and a complete migration rollback catalog (no
  `unknown`). Backup readiness advances to
  `passed_with_non_production_limitations`. No production backup/restore, no real
  cloud write, no real schedule, no raw key persisted; `production_executed=false`
  (Step 49). See `docs/operations/backup-dr-gap-closure.md`.

* Admin Console v1 Operator Actions: a controlled Operator Console at `/operator`
  with test-local signed-session auth, RBAC (viewer/reviewer/operator/
  platform_admin), CSRF, policy-engine gate, one-time confirmation, idempotency,
  and audit. Enabled actions: add note, request changes, accept, reject, and
  allowlisted verification rerun. Delivery acceptance is a human-review
  acceptance only — it never triggers GitHub / PR / merge / deploy / external
  delivery / production. High-risk actions are disabled-only catalog entries;
  `production_executed=false` (Step 50). See
  `docs/product/admin-console-v1-operator-actions.md`.

## Still out of scope (carry-forward)

* Kubernetes / Helm / ArgoCD runtime baseline (Step 51); production OIDC /
  external IdP integration.
* Work-item dispatch to a real implementing agent.
* Real LLM, real GitHub production write, real deploy, real escalation.
* Real production secret store, real off-host cloud backup target, real
  production backup schedule, real production restore, real pager.
