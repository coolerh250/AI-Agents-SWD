# Frontend → Integration Handoff Template

> Append an entry to `docs/handoffs/<stage>/handoff-index.md` using this shape when Codex hands
> completed frontend work off to Claude Code for review/integration/deployment.

## Handoff: Frontend → Integration

- Stage: `<stage id>`
- Date:
- From: Codex
- To: Claude Code
- Frontend artifacts: link to `docs/frontend/<stage>/*`
- PR link:
- Test evidence: link to `docs/frontend/<stage>/test-evidence.md`
- Known gaps:
- Status: `ready for review` / `blocked` (state blocker if blocked)

---
_Non-production only. No production action. No production data. Do not include internal IP
addresses, SSH aliases, private hostnames, real tokens, credentials, private URLs, or environment
secrets — use neutral labels such as "test host", "internal test runtime", "admin console local
tunnel", "sandbox repo"._
