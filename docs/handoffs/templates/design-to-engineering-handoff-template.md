# Design → Engineering Handoff Template

> Append an entry to `docs/handoffs/<stage>/handoff-index.md` using this shape when Claude Design
> hands a design off to Claude Code.

## Handoff: Design → Engineering

- Stage: `<stage id>`
- Date:
- From: Claude Design
- To: Claude Code
- Design artifacts: link to `docs/design/<stage>/*`
- Open questions carried into the contract:
- Status: `ready` / `blocked` (state blocker if blocked)

---
_Non-production only. No production action. No production data. Do not include internal IP
addresses, SSH aliases, private hostnames, real tokens, credentials, private URLs, or environment
secrets — use neutral labels such as "test host", "internal test runtime", "admin console local
tunnel", "sandbox repo"._
